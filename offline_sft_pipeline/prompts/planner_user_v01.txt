Current planning context:

sample_id: $sample_id
trajectory_id: $trajectory_id
round_idx: $round_idx
timestamp_utc: $now_iso
requested_suggestion_count: $requested_suggestion_count

Question:
$question

Current visible images:
$visible_images_json

Conversation history:
$messages_json

Remaining depth:
$budget_json

Latest runtime result:
$latest_runtime_result_json

Available capabilities:
$tool_capabilities_json

Decide whether the question can be answered now.
If yes, return one JSON object with mode="answer", plus think and answer.
If no, return one JSON object with mode="suggestions", plus think and suggestions.
